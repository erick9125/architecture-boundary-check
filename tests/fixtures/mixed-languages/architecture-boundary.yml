version: 1

root: "src"

layers:
  - name: domain
    paths:
      - "domain/**"

  - name: application
    paths:
      - "application/**"

  - name: infrastructure
    paths:
      - "infrastructure/**"

rules:
  - from: application
    cannotDependOn:
      - infrastructure
