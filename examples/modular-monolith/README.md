# Modular monolith example

Feature modules may depend on `shared`. `shared` cannot depend on feature modules.

Public/internal module APIs are planned for 0.2.0. In 0.1.0, boundaries are path-based.
